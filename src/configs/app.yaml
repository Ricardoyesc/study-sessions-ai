server:
  port: 8080

database:
  host: localhost
  port: 5432
  name: sai_learning
  user: sai_user
  password: change-me

redis:
  url: localhost:6379

minio:
  endpoint: localhost:9000
  access_key: minioadmin
  secret_key: minioadmin

llm:
  provider: openai
  model: gpt-4o

tts:
  provider: gemini

image:
  provider: dalle
